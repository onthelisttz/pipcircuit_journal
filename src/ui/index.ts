/**
 * UI Layer - Barrel Export
 *
 * This layer contains all React components, hooks, state, and providers.
 * It depends on the application layer via dependency injection (hooks/factories).
 */

// Components
export * from "./components";

// Feature Views
export * from "./features";

// Hooks
export * from "./hooks";

// State (Zustand)
export * from "./state";

// Providers
export * from "./providers";
